# Required for AI features (Phase 2). Without a key the app runs on a built-in mock provider.
GROQ_API_KEY=

# Optional overrides
# TASKER_DB=sqlite:///./tasker.db
# TASKER_EXTRACTION_MODEL=llama-3.3-70b-versatile
# TASKER_INTERVIEW_MODEL=llama-3.1-8b-instant
# TASKER_EMBEDDER=local        # 'mock' = deterministic test embedder (no model download)
